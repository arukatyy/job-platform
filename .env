# PostgreSQL connection
PGUSER=postgres
PGPASSWORD=123
PGHOST=localhost
PGPORT=5432
PGDATABASE=bd

# Optional: use a single connection string instead
# DATABASE_URL=postgres://user:pass@host:5432/dbname